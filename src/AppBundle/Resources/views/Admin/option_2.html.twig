{% form_theme form 'form_table_layout.html.twig' %}


{% block stylesheets %}
    <style>
table#form {
    font-size:25px;
    }
button {
    font-size:25px;
}
        tr {
            height:35px;
        }
        input[type=checkbox]{
            transform: scale(1.5);

            /* Webkit browsers*/
            -webkit-transform: scale(1.5);
        }
    </style>
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('adminpage') }}">Natrag</a>
{% endblock %}